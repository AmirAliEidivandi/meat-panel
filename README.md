# تاریخچه سیستم مدیریت

این پروژه یک رابط کاربری زیبا و مدرن برای نمایش تاریخچه‌های مختلف سیستم مدیریت
است.

## ویژگی‌ها

- 🔐 **احراز هویت امن**: ورود با شماره موبایل و رمز عبور
- 📱 **طراحی ریسپانسیو**: سازگار با تمام دستگاه‌ها
- 🎨 **UI مدرن**: طراحی زیبا با Tailwind CSS
- 📊 **تاریخچه کامل**: نمایش تاریخچه کیف پول، سفارشات، پرداخت‌ها و فاکتورها
- 🔍 **جستجو و فیلتر**: امکان جستجو و فیلتر کردن تاریخچه‌ها
- 🔄 **بروزرسانی خودکار**: قابلیت بروزرسانی داده‌ها

## نصب و راه‌اندازی

### پیش‌نیازها

- Node.js (نسخه 18 یا بالاتر)
- npm یا yarn

### مراحل نصب

1. **نصب dependencies:**

```bash
npm install
```

2. **اجرای پروژه:**

```bash
npm run dev
```

3. **دسترسی به پروژه:** پروژه در آدرس `http://localhost:5173` در دسترس خواهد
   بود.

## ساختار پروژه

```
src/
├── components/          # کامپوننت‌های UI
│   ├── Login.tsx       # صفحه ورود
│   ├── Sidebar.tsx     # منوی کناری
│   ├── WalletHistory.tsx    # تاریخچه کیف پول
│   ├── OrderHistory.tsx     # تاریخچه سفارشات
│   ├── PaymentHistory.tsx   # تاریخچه پرداخت‌ها
│   └── InvoiceHistory.tsx   # تاریخچه فاکتورها
├── services/           # سرویس‌های API
│   └── api.ts          # مدیریت درخواست‌های HTTP
├── types/              # تعریف انواع TypeScript
│   └── index.ts        # انواع داده‌ها
├── lib/                # ابزارهای کمکی
│   └── utils.ts        # توابع کمکی
└── App.tsx             # کامپوننت اصلی
```

## API Endpoints

### احراز هویت

- `POST /auth/login` - ورود به سیستم
- `POST /auth/refresh` - تازه‌سازی توکن

### تاریخچه‌ها

- `GET /wallet-history` - تاریخچه کیف پول
- `GET /order-history` - تاریخچه سفارشات
- `GET /payment-history` - تاریخچه پرداخت‌ها
- `GET /invoice-history` - تاریخچه فاکتورها

## ویژگی‌های UI

### صفحه ورود

- فرم ورود با شماره موبایل و رمز عبور
- نمایش/مخفی کردن رمز عبور
- مدیریت خطاها
- انیمیشن‌های زیبا

### سایدبار

- منوی ناوبری با آیکون‌های زیبا
- نمایش وضعیت فعال
- دکمه خروج از سیستم
- طراحی ریسپانسیو

### صفحات تاریخچه

- نمایش کارت‌های زیبا برای هر تاریخچه
- جستجو و فیلتر پیشرفته
- نمایش جزئیات کامل
- آیکون‌های معنادار
- رنگ‌بندی مناسب برای وضعیت‌های مختلف

## تکنولوژی‌های استفاده شده

- **React 19** - کتابخانه UI
- **TypeScript** - زبان برنامه‌نویسی
- **Tailwind CSS** - فریمورک CSS
- **Vite** - ابزار ساخت
- **Axios** - مدیریت درخواست‌های HTTP
- **Lucide React** - آیکون‌های زیبا
- **React Router** - مدیریت مسیریابی

## تنظیمات

### تغییر آدرس API

برای تغییر آدرس API، فایل `src/services/api.ts` را ویرایش کنید:

```typescript
const API_BASE_URL = 'http://localhost:3301'; // آدرس API خود را اینجا قرار دهید
```

### سفارشی‌سازی رنگ‌ها

برای تغییر رنگ‌ها، فایل `tailwind.config.js` را ویرایش کنید:

```javascript
theme: {
	extend: {
		colors: {
			primary: {
				// رنگ‌های اصلی خود را تعریف کنید
			}
		}
	}
}
```

## توسعه

### اضافه کردن کامپوننت جدید

1. فایل کامپوننت را در پوشه `src/components/` ایجاد کنید
2. کامپوننت را در `App.tsx` import کنید
3. در تابع `renderContent` case جدید اضافه کنید

### اضافه کردن API جدید

1. endpoint جدید را در `src/services/api.ts` اضافه کنید
2. نوع داده را در `src/types/index.ts` تعریف کنید
3. از سرویس در کامپوننت استفاده کنید

## عیب‌یابی

### مشکلات رایج

1. **خطای CORS**: مطمئن شوید که سرور API CORS را پیکربندی کرده است
2. **خطای 401**: توکن منقضی شده، دوباره وارد شوید
3. **خطای شبکه**: آدرس API را بررسی کنید

### لاگ‌ها

برای مشاهده لاگ‌ها، Developer Tools مرورگر را باز کنید و به تب Console بروید.

## مشارکت

برای مشارکت در پروژه:

1. Fork کنید
2. شاخه جدید ایجاد کنید
3. تغییرات خود را commit کنید
4. Pull Request ارسال کنید

## مجوز

این پروژه تحت مجوز MIT منتشر شده است.
